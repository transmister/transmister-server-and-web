import { PrimaryButton } from "office-ui-fabric-react"

export default () => {
    return <div style={{width: "100%", height: "100vh", display: "flex", justifyContent: "center", alignItems: "center"}}><PrimaryButton>Conversations</PrimaryButton></div>
}